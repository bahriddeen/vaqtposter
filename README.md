# VaqtPoster

Prayer-time poster generator built with React, TypeScript, and SVG.

## Run locally

```bash
npm install
npm run dev
```

Create a production build with `npm run build`.

All poster rendering and exports happen locally in the browser. Uploaded images are not sent to a server.
